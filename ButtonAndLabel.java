/*
package com.example.buttonandlabel;

import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.layout.FlowPane;
import javafx.stage.Stage;


public class ButtonAndLabel extends Application{


    public static void main(String[] args) {
        System.out.println("Hello world!");
        launch(ButtonAndLabel.class);
    }

    @Override
    public void start(Stage window) {

        Button button = new Button("kliknij tutaj");
        FlowPane componentGroup = new FlowPane();
        componentGroup.getChildren().add(button);

        Label textComponent = new Label("kliknij na ten guzik");
        componentGroup.getChildren().add(textComponent);

        Scene view = new Scene(componentGroup);

        window.setTitle("ButtonAndLabelApplication");
        window.setScene(view);
        window.show();
    }
}
*/
